## Sentiment Analysis Project

# First steps to run the app:
# npm install
# pip install -r REST/requirements.txt

# To run the app you need two terminals. In the first one to get the REST api running:
# uvicorn REST.api:app --reload

# In second terminal to run the website run: 
# npm run dev
